# Reference SQL

These scripts are legacy references from the pre-migration setup.
They are not executed automatically by Spring Boot.

- `schema-reference.sql`: full schema snapshot
- `seed-reference.sql`: sample data snapshot
